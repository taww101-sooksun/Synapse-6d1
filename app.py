import streamlit as st
from streamlit_js_eval import get_geolocation

# 1. ตั้งค่าพิกัดเป้าหมาย (จุดที่ต้องตรงถึงจะจริง)
TARGET_LAT = 13.72000  # อ่อนนุช 65 (ระบุเลขจริงของคุณ)
TARGET_LON = 100.65000
ALLOWED_DRIFT = 0.0005  # ระยะคลาดเคลื่อนที่ยอมรับได้ (ประมาณ 50 เมตร)

st.title("🛡️ MATRIX_V2: ระบบตรวจสอบความจริง")

# 2. ดึงค่า GPS สดจากเครื่อง
loc = get_geolocation()

if loc:
    curr_lat = loc['coords']['latitude']
    curr_lon = loc['coords']['longitude']
    
    # 3. ตรวจสอบความจริงของตำแหน่ง
    lat_diff = abs(curr_lat - TARGET_LAT)
    lon_diff = abs(curr_lon - TARGET_LON)
    
    if lat_diff < ALLOWED_DRIFT and lon_diff < ALLOWED_DRIFT:
        st.success("✅ ตำแหน่งตรงตามความจริง - ระบบ MATRIX เริ่มการหักล้าง")
        # ใส่โค้ดคำนวณรหัส 44/252 ของคุณตรงนี้
        st.write(f"กำลังประมวลผลมิติ ณ พิกัดจริง: {curr_lat}, {curr_lon}")
    else:
        st.error("🚨 อันตราย: ตำแหน่งไม่ตรงตามความจริง!")
        st.warning("ระบบถูกยกเลิก (Aborted) เพื่อป้องกันความผิดพลาดเชิงมิติ")
        st.info("กรุณากลับเข้าสู่จุดศูนย์กลางเพื่อเริ่มระบบใหม่")
        # หยุดการทำงานทั้งหมด
        st.stop() 
else:
    st.info("💡 กรุณาเปิด GPS และกดอนุญาต เพื่อยืนยันตัวตนผ่านตำแหน่ง")
