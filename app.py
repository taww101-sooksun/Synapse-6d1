import streamlit as st
import numpy as np

st.title("🎤 MATRIX_V2: Double Code Fusion")
st.write("รหัส 6802721088 x 61244252 | สภาพการทำงาน: นิ่งระดับสูงสุด")

if st.button("🔥 รันพิกัดผสม (Sync Now)"):
    sr = 44100
    t = np.linspace(0, 8, sr * 8)
    
    # --- ฐาน 61244252 (The Stillness) ---
    base_147 = np.sin(147 * 2 * np.pi * t) * 0.4 # Kick & Bass
    
    # --- ส่วนขยาย 6802721088 (The Harmony) ---
    # ใช้ 680Hz เป็นทำนองหลัก และ 1088Hz เป็นลูกเล่น
    lead_680 = np.sin(680 * 2 * np.pi * t) * 0.1
    sparkle_1088 = np.sin(1088 * 2 * np.pi * t) * 0.05
    
    # สร้างจังหวะขยัก (Stutter) ตามสไตล์ Hip Hop
    stutter = np.where(np.sin(90 * 2 * np.pi * t) > 0, 1, 0)
    
    final_mix = (base_147 + (lead_680 * stutter) + sparkle_1088) * 0.3
    
    st.audio(final_mix, sample_rate=sr)
    st.success("รวมรหัสสำเร็จ! มิติเสียงมีความหนาแน่นและชัดเจนขึ้น 200%")
