import streamlit as st
import pandas as pd
import numpy as np
import time
from streamlit_js_eval import get_geolocation

   # ... โค้ดส่วนบนของคุณ ...

if loc:
    # (โค้ดส่วนที่ทำงานเมื่อได้ GPS)
    st.success("แชร์ตำแหน่งสำเร็จ")
    # ตรวจสอบว่ามีโค้ดย่อหน้าอยู่ใต้ if นี้ครบถ้วน
else:
    # บรรทัดที่ 63: หลัง else ต้องมีย่อหน้าลงมาแบบนี้
    st.error("🚨 ระบบหยุดทำงาน: โปรดแชร์ตำแหน่งเพื่อยืนยันความจริง")
    st.info("กรุณากด 'Allow' หรือ 'อนุญาต' เพื่อเข้าสู่สภาวะจริง")
    st.stop() 


if loc:
    # ดึงค่าพิกัดจริงจาก GPS ที่คุณแชร์ให้แอป
    curr_lat = loc['coords']['latitude']
    curr_lon = loc['coords']['longitude']
    accuracy = loc['coords']['accuracy']
    
    st.sidebar.success("✅ กำลังแชร์ตำแหน่งสด")
    st.sidebar.write(f"Lat: {curr_lat:.6f}")
    st.sidebar.write(f"Lon: {curr_lon:.6f}")
    
    # --- จุดพิสูจน์ความจริง ---
    # ถ้าค่า Lat/Lon เปลี่ยน ตัวเลขด้านล่างต้องเปลี่ยนตามทันที
    
    gates = ['A: Stability', 'B: Filtering', 'C: Reflection', 
             'D: Equilibrium', 'E: Silence', 'F: Unity']
    results = []
    ts = time.time() # ดึงเวลาจริงมาใช้ร่วมกับพิกัด
    
    for i, gate in enumerate(gates):
        # ใช้พิกัดจริง + รหัส 44 กุญแจ (จำลองฐาน 252) ในการคำนวณ
        # สมการนี้จะเปลี่ยนค่าทุกครั้งที่ Lat/Lon ขยับ
        g_plus = np.sin(ts + curr_lat + i) * 44 
        s_minus = np.cos(ts + curr_lon + i) * 44
        sc_unit = abs(g_plus + s_minus) / 7.33 # ตัวหารเพื่อให้ได้ฐานสมดุล
        
        results.append({
            "ด่าน (Gate)": gate,
            "G+ (จริง)": round(g_plus, 4),
            "S- (จริง)": round(s_minus, 4),
            "หน่วยวัด (SC)": round(sc_unit, 4)
        })

    df = pd.DataFrame(results)
    
    # แสดงผลตารางความจริง
    st.table(df)
    
    # ระบบแจ้งเตือนความนิ่ง
    if accuracy < 15:
        st.success(f"💎 สภาวะนิ่งสมบูรณ์ (Accuracy: {accuracy:.2f}m)")
    else:
        st.warning(f"⚠️ สัญญาณแกว่ง: ระบบกำลังรอให้คุณอยู่นิ่งๆ (Accuracy: {accuracy:.2f}m)")

    # กราฟแสดงการหักล้างของเวลาและตำแหน่ง
    st.line_chart(df["หน่วยวัด (SC)"])

else:
